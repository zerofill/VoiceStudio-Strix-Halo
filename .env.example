# Required — long random secret for server-mode admin routes
OMNIVOICE_API_KEY=change-me-to-a-long-random-string

# Optional Hugging Face token for gated models
# HF_TOKEN=

# Host group IDs for GPU device access (defaults work on most Linux systems)
# RENDER_GID=$(getent group render | cut -d: -f3)
# VIDEO_GID=$(getent group video | cut -d: -f3)
